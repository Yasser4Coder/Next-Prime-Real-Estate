# Production API URL – used at BUILD time (Vite bakes this into dist/).
# Copy to .env.production (or .env) before running: npm run build
# Do NOT commit .env or .env.production – only this example file.

VITE_API_URL=https://server.nextprimerealestate.com
